[pytest]
# rootdir on sys.path so `from tests.conftest import ...` resolves. Without it the
# tests package is not importable and every module that shares a fixture fails at
# collection.
pythonpath = .
testpaths = tests
addopts = --ignore=tests/postgres_integration --cov=shared --cov=domains --cov-fail-under=90
